# 04. QA 리포트 — D봇(QA/리뷰)

작성: D봇 · 2026-09-11 · 근거: `00-brief.md`, `01-plan.md`(§8 성공 기준), `02-design.md`, `03-dev-log.md`
검수 대상: `site/` 전체, `functions/coach.js`, `tools/check.js` (커밋 `4b02561` + E봇이 QA 중 수정한 미커밋 `<head>`/robots/sitemap 변경분 포함)
환경: Chromium(Playwright), 로컬 `python3 -m http.server`. **주의**: 이 세션은 Google Fonts 접속이 차단되어(`ERR_CONNECTION_RESET`) 모든 스크린샷은 시스템 폰트(WenQuanYi Zen Hei, 한글 세리프 없음)로 렌더됨. 세리프/모노 타이포 인상은 실기기 확인 필요. Lighthouse 미설치 → 대비는 DOM 전수 계산으로 대체.
스크린샷: `/tmp/claude-0/-home-user-ex01/3587d56c-2ffa-5ef3-bdc6-98e60fd70217/scratchpad/qa-shots/` (목록은 §6)

> QA 진행 중(10:25) E봇이 9개 HTML의 `<head>`(meta/OG alt/JSON-LD/PNG favicon 링크)와 robots.txt·sitemap.xml을 수정했다. 본문(body) 카피 변경은 없음(`git diff` 확인). 아래 HTML 라인 번호는 수정 후 파일 기준.

---

## 1. 요약 판정: **수정 후 출시 가능**

- Blocker 0 · Major 6 · Minor 13 · Nit 10 (총 29건).
- `tools/check.js` 재실행: **32/32 페이지×폭 PASS, 47 assert 전부 ok, 실패 0** (호흡 5라운드 30.019s, CALM 콜백 30.57s). C봇 결과 재현됨.
- 그러나 check.js 가 보지 않는 영역에서 Major 6건이 나왔다: AI 코치 재시작 레이스 2건(타이머 누수·pageerror), JS 미동작 시 본문 전체 미표시, 보조 텍스트 대비 미달(4.5:1), 자가진단 키보드 자동 진행(WCAG 3.2.2), 호흡 재개 시 문구 고정.
- 카피 무결성은 우수: 기획 카피(히어로·섹션 헤드라인·마음브레이크 4단계·코치 스크립트 전체·감정 인정 10문장·문항 16개·결과 문구 6개·위기 문구) 원문 일치. 브리프에 없는 연락처·수치·후기·경력은 **없음**(전부 `[교체:]`). 기획 지시문이 UI에 새는 곳 1건(Q-10).
- 디자인 토큰(컬러 hex 전체, 폰트 스택, 타입 스케일, 버튼 52/48, 라운드 999/4/4, 섹션 인덱스 라벨+1px 라인)은 CSS에 그대로 있음. 금지 사항(그라데이션 히어로·좌측 컬러바·이모지·아이콘 나열) 위반 없음.

---

## 2. 01-plan §8 성공 기준 10개

| # | 항목 | 판정 | 근거 |
|---|---|---|---|
| 1 | 7페이지+404가 400/768/1024/1440 에서 가로 스크롤 없음, 헤더 CTA·햄버거 동작 | **PASS** | check.js 32/32. 별도로 `getBoundingClientRect().right > innerWidth` 전수 검사(9페이지×4폭) → 실제 넘침 요소 0(honeypot 오프스크린 제외). 400px 햄버거→오버레이 열림·Esc 닫힘·포커스 복귀 확인(`index-400-menu.png`). 헤더 CTA는 ≤900px 에서 숨기고 오버레이에 노출(기획대로). |
| 2 | 히어로 슬로건이 `h1`, 페이지당 `h1` 1개 | **PASS** | `index.html:93` `<h1 class="display">돈과 관계 앞에서 흔들리는 마음을 훈련합니다.</h1>`. 9페이지 모두 h1=1 (privacy 포함 별도 확인). |
| 3 | 다크/라이트 시스템 반영 + 토글 저장, 두 테마 본문 대비 4.5:1, Lighthouse ≥95 | **부분** | 시스템 다크 에뮬레이션 → `data-theme` 없이 bg `#15130F`, 토글 `aria-pressed=true`, 달 아이콘 표시 ✓. 토글→localStorage `hm-theme` 저장·새로고침 유지 ✓(check.js). 본문 `--ink/--bg` 15.99:1(라이트)/15.77:1(다크) ✓. **그러나** `--ink-3` 캡션·라벨(12~14px)이 3.21:1(라이트)/4.11:1(다크), 주의 구간 라벨 `--amber` 2.42:1, `.eyebrow--relation` 4.45:1, 다크 최종 CTA lead 3.85:1 → 페이지당 9~36개 텍스트 노드가 4.5:1 미달(Q-04). Lighthouse 미설치로 점수 미측정 — 대비 감사(color-contrast)는 확실히 실패하므로 ≥95 는 보장 불가. |
| 4 | AI 코치 v1 네트워크 차단 상태에서 3트랙 완주 + 요약 카드, 안전 키워드 시 위기 카드 즉시 | **PASS** | 외부 요청 전부 차단한 컨텍스트에서 관계(실제 30초 호흡)·머니·일반 완주, 요약 카드(`7 → 4`/`7 → 3`) 생성(check.js + `ai-coach-1280-summary.png`). 코치 전 흐름 중 네트워크 요청 캡처: 동일 출처 정적 파일 외 **0건**. "끝내고 싶" 입력 → 봇 응답 전 `.safety-card[role=alert]` 즉시(tel:109·1577-0199·관계 1366), 흐름 일시정지→계속 진행 ✓. 무력감+두려움 → 머니 1336 ✓. 단 재시작 레이스 Q-01/Q-02 존재. |
| 5 | 자가진단: 8문항×2, 미응답 차단, 경계값 17/18·28/29, 결과 문구 6개 | **PASS** | `HMCheckup.score` S=17→안정(72), 18→주의(69), 28→주의(38), 29→훈련(34), 8→100, 40→0 ✓. 묶음 동점 우선순위 ✓. `#result=R-10/20/35, M-10/20/35` 6개 헤드라인 상이 ✓. 미응답 시 결과 버튼 disabled + "1번 문항" 안내·이동 ✓. 문항 16개 원문 diff 일치. 부수 이슈 Q-05·Q-08·Q-09·Q-10·Q-11. |
| 6 | 호흡 30초±0.5, 일시정지/중단/재시작, reduced-motion 스케일 비활성, CALM 콜백 | **부분** | 30.019s(일시정지 0.7s 제외) ✓, stop→idle `0 / 5` ✓, reduced-motion `transform` 미적용·링만 ✓, CALM `onComplete` → 다음 말풍선 30.57s ✓. **FAIL**: 일시정지 후 재개하면 원 안 문구가 '일시정지'에 고정된 채 최대 3초(다음 단계 전환)까지 유지(Q-06). |
| 7 | 폼 2종 인라인 오류, 전화 패턴, honeypot, 제출 확인 메시지(mailto 폴백) | **부분** | 빈 제출 → 오류 5개 + 첫 오류 필드 포커스 + `aria-invalid`/`aria-describedby` ✓, `01012345678` 거부·`010-1234-5678` 통과 ✓, honeypot 채움 → 조용한 성공 ✓, `#lecture` 탭 ✓(check.js·`contact-1280-errors.png`). mailto 폴백은 `data-mailto` 미설정(플레이스홀더)이라 "전송 경로 미설정" 안내 모드로 동작 — 코드 경로(`forms.js:107-111`)는 존재하나 실값 없이는 검증 불가. 이메일 입력 후 재검증 필요. |
| 8 | 키보드만으로 전 페이지·모달·코치 조작, 포커스 링, 모달 트랩, alt, label, 스킵 링크 | **부분** | Tab 순서: 스킵 링크→워드마크→5메뉴→호흡→테마→상담 신청→히어로 CTA, 전 요소 `outline solid 2px` ✓. 스킵 링크 Enter → `main` 포커스 ✓. 호흡 모달: 시작→소리→닫기→라운드 3개→순환 트랩 ✓, Shift+Tab 역순 ✓. 오버레이 11회 Tab 순환 ✓, Esc 복귀 ✓. 코치: 칩 Enter → 슬라이더 화살표 → textarea 입력 → 감정 확인 → 욕구 → CALM 진입 시 `시작` 버튼 자동 포커스 ✓. 라디오 필(폼) Space 선택 + span 에 포커스 링 ✓, 탭 ArrowRight 이동 ✓. `<img>` 없음, 플레이스홀더 `role=img aria-label` ✓, SVG `aria-hidden` ✓, 모든 input `label for`/`aria-label` ✓. **FAIL**: 자가진단 라디오 화살표/Space 즉시 자동 진행(Q-05), 버튼 hidden 전환 시 포커스 body 이탈로 모달 Esc 무반응(Q-07), 코치 칩 선택 후 포커스 상실(Q-19). |
| 9 | 페이지별 title/description/OG(1200×630)/canonical, favicon 세트, sitemap·robots, 콘솔 에러·404 0 | **PASS** | 9페이지 title/description/canonical/og:*/twitter:* 상이 ✓, og-image.png 1200×630 실측 ✓, favicon.svg + 192/512 PNG(E봇 수정으로 `<link>` 및 apple-touch-icon PNG 연결됨) ✓, sitemap 7 URL·robots ✓, JSON-LD 9페이지 파싱 OK. 페이지 로드 콘솔 에러 0·동일 출처 4xx 0(36회 로드). 단 사용자 조작(재시작 레이스) 시 pageerror 1건 재현(Q-02). og-image.png 는 세리프 미적용 상태(Q-15). |
| 10 | 브리프에 없는 사실 없음, `[교체:]` 목록 첨부, 위기 연락처(109·1577-0199·1336·1366) 코치·진단·푸터 노출 | **PASS** | 전 페이지 본문·JSON-LD 대조: 연락처·가격·수치·후기·기관명·연도 등 지어낸 사실 없음(relation.html 사례는 기획 허용 "일반화 시나리오", money.html 워크시트 항목은 사실 아님). 위기 4종: ai-coach 상단 바+푸터, checkup 하단+결과 1336, 전 페이지 푸터 바에 `tel:` 링크 ✓. `[교체:]` 102곳 전수 목록 §7. 예외: 기획 지시문 노출 Q-10. |

---

## 3. 이슈 목록 (심각도순)

### Major

**Q-01 · Major · `site/assets/js/coach.js:150-151, 230-239`** — 재시작 시 호흡 타이머 미파괴 → 새 세션에 유령 말풍선 주입
- 현상: CALM 호흡 진행 중 `처음부터 다시`를 누르면 `chat.innerHTML=''`만 하고 타이머(RAF)는 계속 돈다. 30초 뒤 이전 세션의 `onComplete`가 실행되어 새 세션(진입 화면)에 "호흡 5라운드 완료", "잘하셨어요. 한 번 더…" 말풍선과 선택지 블록이 삽입된다. 재현 결과: `{"bubbles":["안녕하세요…","호흡 5라운드 완료","잘하셨어요…"],"blocks":1}`.
- 재현: `ai-coach.html?track=relation` → 행동 선택 → 감정 확인 → 욕구 → 마음 낮추기 → `시작` → 즉시 `처음부터 다시` → 31초 대기.
- 제안: `start()`에서 `if (root._breath) root._breath.destroy()`; 세션 토큰(`var session = ++seq`)을 만들어 모든 콜백/`then`에서 `if (session !== seq) return;` 가드.

**Q-02 · Major · `site/assets/js/coach.js:50-66`** — 타이핑 인디케이터 중 재시작 → `removeChild` pageerror + 이전 세션 발화 중복
- 현상: `say()` 대기(400~700ms) 중 `start()`가 `chat.innerHTML=''`로 typing 노드를 지운 뒤, 이전 Promise 가 `chat.removeChild(typing)`을 호출해 `Failed to execute 'removeChild' on 'Node'` 에러가 나고, 이어서 `botBubble()`이 옛 발화를 새 세션에 붙인다. 재현: `?track=money` 로드 직후 100ms 내 `처음부터 다시` → 봇 말풍선 "좋아요…", "지금 어떤 행동…"이 2회 출력, pageerror 1건.
- 제안: Q-01의 세션 토큰 가드 + `if (typing.parentNode) chat.removeChild(typing)`.

**Q-03 · Major · `site/assets/js/reveal.js`, `site/assets/css/base.css:236`** — JS 미동작 시 본문 섹션 전체 미표시
- 현상: `[data-reveal]{opacity:0}`가 무조건 적용되어 JS 비활성/스크립트 로드 실패 시 38개 블록(히어로 이하 전 섹션)이 보이지 않음(`index-1440-nojs.png` — 빈 화면). 검색엔진은 JS 렌더하지만 스크립트 오류·CSP·저사양 환경에서 콘텐츠 전멸.
- 제안: `<html class="no-js">` + `<head>` 인라인 `document.documentElement.classList.replace('no-js','js')`, CSS 를 `html.js [data-reveal]{opacity:0;…}` 로 한정. 또는 `@media (scripting: none){[data-reveal]{opacity:1;transform:none}}` 추가.

**Q-04 · Major · `site/assets/css/tokens.css:15,27,89`, `base.css:165,181`, `components.css:96,134-136,189,230,283,290,294`** — 보조 텍스트 대비 4.5:1 미달(라이트·다크 모두)
- 현상(계산값): `--ink-3 #8E887C` on bg 3.21:1 / on bg-2 2.94:1 / on surface 3.47:1 (라이트), `#7C766A` 4.11 / 3.85 / 3.69 (다크). 적용처: `.faint`(문항 번호, "01 STOP" 등), `.eyebrow`가 아닌 `.replace`·`.placeholder`·`.notice`·`.hint`·`.stepper__item`(비활성 단계)·`.tool-card__meta`·`.numlist__num`·`.tech__num`·`.site-footer h2`·`.site-footer__bottom`(13px)·`.likert__ends`·`.gauge__legend`(11px)·checkup 하단 위기 링크(`.small.faint .text-link` 109/1577-0199 가 3.21:1!). 추가: `.result__band--caution`(`--amber #C9963A`) **2.42:1**, `.eyebrow--relation`(#B4553A) 4.45:1, `.form .req` 4.45:1, 다크 `.final .lead`(ink-3 on bg-2) 3.85:1. 페이지당 9~36개 텍스트 노드 실패(`qa-results.json` contrast 항목).
- 제안: 라이트 `--ink-3`를 `#6E6960`(≈4.6:1 on bg) 수준으로, 다크 `#8F887B`(≈5:1)로 조정하고 12px 미만 사용 금지. 앰버 텍스트용 `--amber-ink: #8A6420`(라이트) 분리. relation 을 `#A84D33`(≈5:1)로 미세 조정. 위기 전화 링크(checkup 하단)는 `--danger` 또는 `--ink-2`로.

**Q-05 · Major · `site/assets/js/checkup.js:108`** — 라디오 `change` 즉시 자동 진행 → 키보드 사용자 문맥 강제 이동(WCAG 3.2.2)
- 현상: 화살표/Space 로 라디오를 고르는 순간 180ms 뒤 다음 문항 카드로 전환되고 포커스가 다음 h2 로 이동(재현: q0 에서 ArrowRight → 카드 1 표시, q0=2 선택됨, 포커스 H2). 화살표로 1→2→3 탐색 자체가 불가능(첫 이동에서 문항이 넘어감). 마우스 사용자에게는 편의지만 키보드/스크린리더에는 예기치 않은 문맥 변경.
- 제안: `pointer` 입력(`click` 이벤트 + `e.detail>0` 또는 `pointerType`)에서만 자동 진행, 키보드 변경은 `다음` 버튼 또는 Enter 로 진행. 최소한 자동 진행 전 `aria-live`로 "다음 문항으로 이동합니다" 안내.

**Q-06 · Major · `site/assets/js/breath.js:153-163`** — 일시정지→재개 시 원 안 문구 '일시정지' 고정
- 현상: `pause()`가 `phase.textContent='일시정지'`로 바꾸지만 `lastPhase`를 초기화하지 않아 재개 후 `render()`의 `ph !== lastPhase` 가 거짓 → 다음 들숨/날숨 전환(최대 3초)까지 '일시정지'가 남고 `aria-live` 낭독도 없음. 재현 로그: `pause text='일시정지' → 400ms after resume text='일시정지'`.
- 제안: `start()`의 paused 분기에서 `lastPhase=''` 리셋(또는 `pause()`에서 별도 오버레이 텍스트 사용).

### Minor

**Q-07 · Minor · `site/assets/js/breath.js:118-126, 238-246`** — 컨트롤 버튼 hidden 전환 시 포커스가 body 로 이탈 → 모달 Esc 무반응
- 현상: 키보드로 `시작`(또는 `일시정지`) 활성화 시 그 버튼이 `hidden` 되어 `activeElement=body`. 모달 keydown 리스너는 모달 요소에 붙어 있어 이 상태에서 Esc 를 눌러도 닫히지 않음(재현: Space 로 시작 후 Esc → `.modal` 잔존). Tab 을 한 번 누르면 다시 모달 내부로 들어가 이후엔 정상.
- 제안: `setControls()` 후 상태별로 보이는 첫 버튼(`pauseBtn`/`startBtn`/`againBtn`)에 `focus()`; Esc 리스너를 `document`에 등록하고 모달 닫힘 시 해제.

**Q-08 · Minor · `site/assets/js/checkup.js:223`, `base.css`** — 결과 화면이 스티키 헤더 아래로 스크롤되어 게이지 상단 가림
- 현상: `resultEl.scrollIntoView({block:'start'})` 후 `[data-result]` top = -16px, 헤더 61px → 반원 게이지 윗부분이 헤더에 가려짐(`checkup-1280-result.png`, `checkup-768-result.png`). `scroll-padding-top` 미설정(`auto`).
- 제안: `html{scroll-padding-top: calc(var(--header-h-compact) + 16px)}` + `[data-result],[id]{scroll-margin-top: …}`.

**Q-09 · Minor · `site/assets/css/components.css:275`** — 자가진단 `fieldset.likert` 브라우저 기본 테두리 노출
- 현상: `.form fieldset{border:0}`만 있고 `.likert`에는 리셋이 없어 2px groove 테두리 + 12px 패딩 + 2px 마진이 1~5 세그먼트를 감싸 보임(`checkup-1280-question.png`, `checkup-400-question.png`). 02-design §4(진행 바·세그먼트만)와 불일치.
- 제안: `fieldset.likert{border:0;padding:0;margin:0;min-width:0}`.

**Q-10 · Minor · `site/assets/js/checkup.js:44`** — 결과 추천 프로그램 문구에 기획 지시문 노출
- 현상: 머니 '훈련 필요' 추천 프로그램이 "머니 마음근력 개인 코칭 [교체: 회기] · 상담 신청 우선 · **도박 관련 시 1336 안내 병기**"로 렌더(`checkup-768-result.png`). 01-plan 표의 마지막 구는 구현 지시이며 사용자 카피가 아님(1336 crisis-note 는 별도로 이미 렌더됨).
- 제안: `program` 문자열에서 "· 도박 관련 시 1336 안내 병기" 제거.

**Q-11 · Minor · `site/assets/js/checkup.js:193-194`** — 공유 결과(`#result=`)의 '{묶음}' 치환 비문
- 현상: 관계 주의 본문이 "가장 높게 나온 **'먼저 훈련할'** 단계부터 훈련하면…"으로 출력(dev-log §4-4 의 의도 "먼저 훈련할 단계부터"와 다름). 머니 주의도 "'먼저 훈련할' 단계부터".
- 제안: 공유 모드용 문장을 별도 키(`bodyShared`)로 두거나 `'{group}' 단계부터` 전체를 치환(`"가장 높게 나온 '{group}' 단계부터" → "먼저 훈련할 단계부터"`).

**Q-12 · Minor · `site/index.html:194-209`, `pages.css:46-47,57-61`** — ≤900px 에서 MONEY FIT 헤딩이 카드 3장 아래 배치
- 현상: `.track--flip`은 DOM 상 카드가 먼저, aside(h2·흐름도·CTA)가 뒤. 데스크톱은 `order`로 반전되지만 ≤900px 에서 `order:0` → 카드→헤딩 순(768px 실측 headingTop 4758 > cardsTop 4278, `index-768.png`). 스크린리더 읽기 순서도 섹션 제목(`aria-labelledby=money-title`)이 콘텐츠 뒤.
- 제안: DOM 은 항상 aside 먼저 두고, `.track--flip .track__aside{order:2}` 를 `@media (min-width:901px)` 안에서만 적용.

**Q-13 · Minor · `site/about.html:112-117`, `components.css:161-171`** — 철학 섹션 세로 rail 의 좌측 1px 선 불일치
- 현상: 인라인 `style="grid-template-columns:1fr"`로 1열화했으나 `.rail__step{border-left:1px}`(첫 항목만 0)이 그대로 남아 2~4단계 왼쪽에만 세로선이 생김(`about-1440.png` 두 번째 크롭, x≈830).
- 제안: `.rail--vertical` 수정자 클래스를 만들어 모바일 규칙(border-left:0; border-top:1px) 재사용, 인라인 style 제거.

**Q-14 · Minor · `site/assets/css/components.css:246,254-256`** — 코치/홈 소형(sm) 호흡 원의 "3초 들숨 · 3초 날숨" 텍스트가 원 테두리에 닿음
- 현상: `.breath--sm .breath__circle` 160px, `.breath__count`(14px 모노, 자간 .08em) 폭 158px → 좌우 1px 여백(`ai-coach-1280-calm.png`).
- 제안: `.breath--sm .breath__count{font-size:12px;letter-spacing:.04em}` 또는 idle 문구를 "3초 들숨 · 3초 날숨" → "들숨 3초 · 날숨 3초" 두 줄로.

**Q-15 · Minor · `site/assets/img/og-image.png`** — OG 이미지 세리프 미적용(02-design §6 "세리프 슬로건")
- 현상: 산세리프 폴백으로 렌더된 PNG(dev-log 도 인지). 공유 카드 첫인상이 디자인과 다름.
- 제안: 폰트 접속 가능한 환경에서 `node tools/make-images.js` 재실행(SVG 자체는 정상).

**Q-16 · Minor(v2 배포 시 Major) · `functions/coach.js:79,130`** — CORS `*` + 우회 가능한 인메모리 레이트리밋
- 현상: `Access-Control-Allow-Origin: *` 로 어떤 사이트에서든 브라우저로 호출 가능. 레이트리밋 키가 `x-forwarded-for`(클라이언트가 위조 가능)이며 웜 인스턴스별 메모리라 서버리스 스케일아웃 시 무효. 세션당 12회 제한은 클라이언트에만 있음. API 키 노출은 없음(서버 env 만 사용) ✓. 입력 검증(track/step/500자)·키워드 사전검사·refusal 처리 ✓.
- 제안: `Origin` 화이트리스트(`https://huggingmind.kr`), 플랫폼 레이트리밋(Netlify/Vercel edge 또는 KV 카운터), 간단한 서명 토큰 또는 Turnstile.

**Q-17 · Minor · `functions/coach.js:84-99`** — `state` 문자열 길이 미제한 및 비객체 payload
- 현상: `state.action/need/choice`를 길이 제한 없이 `JSON.stringify` 해 프롬프트에 삽입(토큰 비용·프롬프트 인젝션 표면 확대). `payload`가 `null`/배열이면 구조분해에서 throw → 500(catch 되긴 함).
- 제안: 각 문자열 `String(x).slice(0,120)`, `intensity`는 0~10 정수 검증, `typeof payload==='object'&&payload` 검사 후 400. 참고: SDK 사용(`client.beta.messages.create` + `betas:['server-side-fallback-2026-07-01']`, `fallbacks:'default'`, `thinking:{type:'adaptive'}`, `output_config:{effort:'low',format:{type:'json_schema',…}}`, TS `timeout` ms, `Anthropic.RateLimitError/APIConnectionError`)은 claude-api 스킬 문서와 일치. 실제 호출은 미배포로 미검증. `.env.example`의 `COACH_MODEL`은 코드에서 읽지 않음(Nit).

**Q-18 · Minor · `site/assets/js/coach.js:47,158,168`** — 칩 선택 후 포커스 상실
- 현상: `clearBlocks()`가 포커스된 칩을 제거해 `activeElement=body`(재현: "after Enter, active=BODY"). Chrome 은 순차 포커스 시작점을 유지해 다음 Tab 은 새 CTA 로 가지만, 스크린리더 사용자는 위치를 잃고 `aria-live` 낭독에만 의존.
- 제안: 새 블록 렌더 후 첫 인터랙티브 요소 또는 직전 봇 말풍선(`tabindex=-1`)에 `focus({preventScroll:true})`.

**Q-19 · Minor · `site/index.html:38`(전 페이지 동일), `components.css`** — Google Fonts CSS 렌더 차단 + 3패밀리 7웨이트
- 현상: `<link rel=stylesheet>` 폰트 CSS 가 렌더 차단(preconnect 만 있음). 한글 서브셋(Noto Sans/Serif KR) 슬라이스가 수십 개 로드되어 첫 화면 폰트 비용이 큼(`display=swap` ✓). 이 세션에서는 폰트 서버 접속 실패 시 페이지는 정상 렌더(폴백) — 오류 없음 ✓.
- 제안: `<link rel="preload" as="style" onload>` 패턴 또는 `media="print" onload="this.media='all'"`, 웨이트를 400/700(Sans)·600(Serif)·400(Mono)로 축소, self-host + `unicode-range` 검토.

### Nit

**Q-20 · Nit · `site/assets/css/components.css:143-150`** — `.flow__arrow` 클래스 미사용(HTML 은 `.icon`) → 화살표 색 `--ink-3` 의도 미적용(현재 ink). `.tag--accent`, `.chat__field`(인라인 style 로 대체) 미사용, `checkup.html:246` `.mb` 미정의. 인라인 `style` 7곳(about 2, checkup 1, index 2, relation/money final 각 1).

**Q-21 · Nit · `site/index.html:131,140`, `components.css:144-150`** — 400px 에서 흐름도가 3행으로 줄바꿈될 때 "여기가 개입지점입니다" 라벨이 절대배치되어 2행의 `관계 악화`/`추가 손실` 옆에 놓임(현재 겹침은 없으나 소속 행이 모호, `index-400-s2-flow.png`). 좁은 폭에서는 라벨을 흐름 아래 정적 배치로 전환 권장.

**Q-22 · Nit · `site/assets/css/pages.css:27`** — 768px 히어로 포트레이트가 300px 좌측 정렬, 우측 절반 공백(`index-768.png`). `max-width:min(360px,60%)` 또는 중앙 정렬 검토.

**Q-23 · Nit · `site/assets/js/checkup.js:222`** — 공유 링크로 진입 시 `h2.focus()`가 `:focus-visible` 매칭되어 헤드라인 주위 초록 사각 아웃라인 노출(`checkup-1280-result.png`). `[tabindex="-1"]:focus{outline:none}` 또는 `h.focus({focusVisible:false})`.

**Q-24 · Nit · `site/assets/css/components.css:307`** — 폼 입력 포커스 `outline-offset:1px`(디자인 3px) — 의도적이라면 02-design 에 예외 명시.

**Q-25 · Nit · `site/index.html:182-184,196-198`** — 02-design §4 "트랙 카드 하단 Ghost 링크" 가 S4/S5 기술 카드에 없음(S2 카드만 있음). 기획 S4/S5 CTA 는 aside 에 있어 기능상 문제 없음.

**Q-26 · Nit · `site/assets/js/coach-data.js:81`** — NVC 4칸 문구 "~했을 때 (관찰)"(공백 포함) vs 기획 "~했을 때(관찰)". 감정 인정 10문장·나머지 스크립트는 원문 일치.

**Q-27 · Nit · `site/index.html:242`** — S6 호흡 카드 CTA 라벨이 기획 `호흡 시작` 대신 인라인 타이머의 `시작`(기획 "카드 안에서 바로 실행" 조항으로 허용 범위).

**Q-28 · Nit · `functions/.env.example:4`** — `COACH_MODEL` 환경변수를 안내하지만 `coach.js` 는 상수 `MODEL` 만 사용.

**Q-29 · Nit · `tools/check.js:56-64,82-110`** — 검증 공백: (a) `scrollWidth<=innerWidth`만 검사(`body{overflow-x:hidden}`이 넘침을 가릴 수 있음 → 요소 rect 검사 추가), (b) 대비·포커스 가시성·no-JS 미검사, (c) 호흡 재개 문구(Q-06)·재시작 레이스(Q-01/02)·라디오 키보드 진행(Q-05) 미검사, (d) privacy.html 이 PAGES 에서 빠짐. 위 항목을 assert 로 추가하면 회귀 방지 가능.

---

## 4. 카피·디자인 대조 요약

**카피(01-plan ↔ 구현)**: 히어로 h1/서브/바이라인/CTA 2개 ✓ · S2~S8·S10 헤드라인·서브·CTA 라벨 ✓ · 마음브레이크 4단계 동사+설명 ✓ · 하단 3구 태그 ✓ · 코치 진입/STOP/FEEL/CALM/CHOOSE 발화·선택지·마무리·요약 카드 버튼 4개 ✓ · 감정 인정 10문장 전부 일치 ✓ · 욕구 선택지 관계/머니 ✓ · EFT 문장 템플릿 ✓ · 재측정 분기 문구 ✓ · 안전 문구(상시·트리거·머니 1336·관계 1366/112) ✓ · 문항 R1~R8/M1~M8 ✓ · 결과 헤드라인/본문/프로그램 6세트 ✓(예외 Q-10) · 면책 문구 2종 ✓ · 폼 필드/필수 여부/성공 메시지 ✓ · 헤더/푸터 구성 ✓. general 트랙 추가 문구는 dev-log §4-1 에 사유 기록됨. 후기 섹션은 주석 처리(가짜 후기 없음) ✓.

**디자인(02-design ↔ CSS)**: 컬러 18토큰 라이트/다크 hex 전부 일치, `--amber #C9963A` ✓ · 폰트 3스택+Google Fonts URL(`display=swap`) ✓ · 타입 스케일 8단계 clamp/행간/자간 ✓ · 컨테이너 1200/거터 clamp/12컬럼/거터 32→16 ✓ · 섹션 패딩 clamp(80,10vw,144) ✓ · 라운드 999/4/4/50% ✓ · 버튼 52→48(≤900) 패딩 0 28 500 15px ✓, 포커스 2px accent offset 3 ✓ · 헤더 76→60 blur 12 bg 90% ✓ · 섹션 인덱스 라벨 `01 — 문제` + 1px 라인 ✓ · 호흡 원 220/180, 1.35 스케일, 라디얼 ✓ · 말풍선 16/4 라운드 ✓ · 게이지 3색 12px 호 52px 지수 ✓ · 위기 바 danger-soft/14px/밑줄 tel ✓ · 그라데이션은 노이즈·호흡 원 2곳뿐 ✓ · 그림자 1종 ✓ · 이모지 0 ✓ · 인라인 SVG stroke 1.5 ✓. 편차: Q-09(fieldset), Q-13(rail), Q-24(input outline-offset), Q-25(트랙 카드 Ghost), 과도한 여백은 없음(디자인 값 그대로).

---

## 5. 성능·보안 요약

- 전송량(비압축/gzip): index.html 35KB/7.9KB + CSS 4파일 44KB/10.5KB + JS 5파일 19KB/~6.8KB ≈ **98KB / ~25KB** (+ Google Fonts 외부, 한글 서브셋 가변). ai-coach: HTML 17KB + CSS 44KB + JS 49KB(coach+data 30KB) ≈ 110KB. 페이지 내 이미지 0(플레이스홀더 div), favicon.svg 316B. og-image.png 40KB(페이지 미로드).
- 렌더 차단: 폰트 CSS 1 + 로컬 CSS 4(HTTP/2 면 무난, 병합 시 요청 4→1). 스크립트는 body 끝 동기 로드(defer 권장). `.hero::before` feTurbulence 노이즈는 저사양 기기 스크롤 페인트 비용 — 필요 시 정적 PNG 노이즈로 대체.
- 프라이버시: 코치·진단 전 흐름 네트워크 캡처 결과 동일 출처 정적 파일과 폰트 CSS 외 요청 **0건** — "서버 전송 없음" 주장 사실. `sessionStorage`에 요약/결과 코드만 저장(기획 허용).
- 폼: honeypot(`website`, `tabindex=-1`, `aria-hidden`) 동작 ✓, 전송 시 직렬화에서 제외 ✓, `novalidate`+자체 검증 ✓. `counsel-result`의 `pattern`은 novalidate 로 미적용(자체 정규식 검사 없음 — 서버 측 검증 필요).
- v2 함수: 키 서버 전용 ✓, 입력 상한 ✓, 키워드 사전 검사 ✓, refusal 처리 ✓ / CORS·레이트리밋 Q-16, 상태 문자열 Q-17.

---

## 6. 스크린샷 파일 (`scratchpad/qa-shots/`)

페이지×폭(라이트, fullPage): `index-400.png` `index-768.png` `index-1024.png` `index-1440.png` `relation-{400,768,1024,1440}.png` `money-{400,768,1024,1440}.png` `ai-coach-{400,768,1024,1440}.png` `checkup-{400,768,1024,1440}.png` `about-{400,768,1024,1440}.png` `contact-{400,768,1024,1440}.png` `404-{400,768,1024,1440}.png` `privacy-{400,768,1024,1440}.png`
다크(시스템 설정 에뮬레이션): `index-1440-dark.png` `index-400-dark.png` `ai-coach-1440-dark.png` `checkup-1440-dark.png` `contact-1440-dark.png` `relation-1440-dark.png`
상태·흐름: `index-400-menu.png`(오버레이) `index-400-s2-flow.png`(Q-21) `index-1280-breath-modal-running.png` `index-1280-skiplink.png` `index-1280-focus-ring.png` `index-1280-header-compact.png` `index-1440-nojs.png`(Q-03) `ai-coach-1280-feel.png` `ai-coach-1280-calm.png`(Q-14) `ai-coach-1280-safety.png` `ai-coach-1280-summary.png` `ai-coach-400-flow.png` `checkup-1280-question.png`(Q-09) `checkup-400-question.png` `checkup-1280-result.png`(Q-08·Q-23) `checkup-768-result.png`(Q-10) `contact-1280-errors.png` `contact-1280-lecture.png`
데이터: `qa-results.json`(오버플로·대비 실패 목록·키보드 시퀀스·네트워크 캡처), `crops/`(1100px 단위 크롭). C봇 check.js 재실행 결과: `scratchpad/cshots/check-results.json`, 로그 `scratchpad/check-run.log`.

---

## 7. `[교체:]` 지점 전수 목록 (102곳, `grep -o '\[교체:[^]]*\]'`)

| 파일 | 라인 | 항목 |
|---|---|---|
| index.html | 12 | 검색엔진 사이트 인증 메타(주석, E봇 추가) |
| index.html | 104, 106 | 나영채 코치 프로필 사진 3:4 (aria-label + 노트) |
| index.html | 268(×2) | 『상처를 넘어설 용기』 표지 이미지 |
| index.html | 284, 289, 294 | 프로그램 3카드 각 형식(대면·화상 여부)/회기/가격 또는 '문의' |
| index.html | 307–311 | (주석) 실제 수강 후기 1~3, 강의 기관 로고/명칭 |
| index.html | 351, 356 | 푸터 이메일/전화, 상호·대표·사업자등록번호·주소 |
| relation.html | 12 | 사이트 인증 메타(주석) |
| relation.html | 181–183 | 프로그램 3카드 형식/회기/비용 |
| relation.html | 232, 237 | 푸터 이메일/전화, 사업자 정보 |
| money.html | 12 | 사이트 인증 메타(주석) |
| money.html | 174 | 개인 코칭 카드 형식/회기/비용 |
| money.html | 213, 218 | 푸터 이메일/전화, 사업자 정보 |
| ai-coach.html | 12 / 140 / 145 | 인증 메타 / 푸터 이메일·전화 / 사업자 정보 |
| checkup.html | 12 / 131 / 136 | 인증 메타 / 푸터 이메일·전화 / 사업자 정보 |
| about.html | 12 | 인증 메타(주석) |
| about.html | 97(×2) | 프로필 사진 3:4 |
| about.html | 137–139 | 연도별 경력·자격 3행(연도 + 내용) |
| about.html | 149(×2), 153, 154 | 저서 표지 이미지, 출판사 소개문, 구매 링크 |
| about.html | 164 | 강의 기관/주제 목록 |
| about.html | 196, 201 | 푸터 이메일/전화, 사업자 정보 |
| contact.html | 12 | 인증 메타(주석) |
| contact.html | 100 | (주석) 폼 엔드포인트 `data-endpoint`/`data-mailto` |
| contact.html | 101, 138 | 상담/강의 성공 메시지의 회신 소요 시간 |
| contact.html | 118 | 상담 형식 제공 형식 확정 |
| contact.html | 168 | 예산 구간 |
| contact.html | 170 | 강의 소개서 PDF |
| contact.html | 182–185 | 연락 정보: 이메일/전화/운영시간/위치·화상 여부 |
| contact.html | 215, 220 | 푸터 이메일/전화, 사업자 정보 |
| 404.html | 10 / 115 / 120 | 인증 메타 / 푸터 이메일·전화 / 사업자 정보 |
| privacy.html | 10 | 인증 메타(주석) |
| privacy.html | 89 | 사업자 정보·개인정보 보호책임자 |
| privacy.html | 95 | 보유 기간 |
| privacy.html | 97 | v2 활성화 시 처리 위탁 사항 |
| privacy.html | 99(§5) | 폼 엔드포인트·API 제공자 위탁 내역 (`[교체: 폼 엔드포인트(Formspree/Netlify 등)…]`) |
| privacy.html | 101, 103 | 요청 창구 이메일, 시행일 |
| privacy.html | 128, 133 | 푸터 이메일/전화, 사업자 정보 |
| assets/js/checkup.js | 24, 25, 43, 44 | 추천 프로그램 회기(관계 주의/훈련, 머니 주의/훈련) |
| assets/js/forms.js | 114, 115 | 미설정 안내 문구의 엔드포인트/이메일 |
| assets/js/coach.js | 6 | `COACH_ENDPOINT`(v2 활성화 시) |
| assets/img | — | og-image.png 폰트 환경 재생성(Q-15), 인물·저서 이미지 파일 자체 |

배포 전 사용자 입력 필수(화면 노출): 프로필 사진·저서 표지·프로그램 형식/회기/비용·연락처(이메일/전화/운영시간/위치)·사업자 정보·개인정보처리방침 4항목·폼 엔드포인트 또는 이메일·회신 소요 시간·예산 구간·소개서 PDF·연도별 경력·강의 목록·출판사 소개문·구매 링크·추천 프로그램 회기. 자료 없으면 후기 섹션(주석)은 그대로 비노출.

---

## 8. 5줄 요약

1. 판정 **수정 후 출시 가능** — Blocker 0 · Major 6 · Minor 13 · Nit 10. `tools/check.js` 재실행 32/32·47 assert 통과로 C봇 결과는 재현됐으나 check.js 밖에서 Major 6건 발견.
2. Major: 코치 재시작 레이스 2건(타이머 누수로 유령 말풍선·`removeChild` pageerror), JS 미동작 시 본문 전체 비표시, `--ink-3`/amber 등 보조 텍스트 대비 4.5:1 미달(페이지당 9~36 노드), 자가진단 라디오 키보드 자동 진행(WCAG 3.2.2), 호흡 재개 시 '일시정지' 문구 고정.
3. 성공 기준 10개: PASS 6(1·2·4·5·9·10) / 부분 4(3 대비·Lighthouse, 6 재개 문구, 7 mailto 실값 미검증, 8 포커스 관리).
4. 카피 원문 일치·지어낸 사실 없음·위기 연락처 4종 노출·디자인 토큰 일치 확인; 기획 지시문 1건("도박 관련 시 1336 안내 병기")이 UI에 노출.
5. `[교체:]` 102곳 전수 첨부, 스크린샷 60장 + 대비/오버플로/키보드/네트워크 JSON 첨부. 우선 수정 순서: Q-01/02 → Q-04 → Q-05 → Q-03 → Q-06 → Minor.

---

## Round 2 재검증 — D봇

작성: D봇 · 2026-09-11 · 대상 커밋 `6fca1e9`(C봇 R2) · 근거 `03-dev-log.md` "## Round 2" 처리표
환경: 1라운드와 동일(Chromium/Playwright, 로컬 정적 서버, Google Fonts 차단 → 시스템 폰트 렌더, Lighthouse 미설치). 스크린샷 62장: `scratchpad/qa-shots-r2/` (목록 §R2-6). 원데이터 `qa-shots-r2/qa-r2-results.json`, `qa-r2c-results.json`. C봇 check.js 재실행 로그 `scratchpad/check-run-r2.log`, 결과 `scratchpad/cshots-r2/check-results.json`.

### R2-0. 최종 판정: **출시 가능** (사용자 자료 `[교체:]` 입력 · og-image 재생성 전제)

- 1라운드 이슈 29건: **닫힘 25 · 의도적 예외 3(Q-24·Q-25·Q-27, 근거 타당) · 환경 제약 1(Q-15)**. 회귀 0.
- Major 6건 전부 Playwright 로 직접 재현해 닫힘 확인(§R2-1). 라이트 9페이지×2폭 + 다크 9페이지 + 결과 화면 8종 텍스트 노드 **전수 4.5:1 통과(실패 0)**.
- `tools/check.js`(R2 보강판) 재실행: **36/36 PASS · assert 73 ok · 실패 0** · 호흡 30.02s · CALM 콜백 30.59s — C봇 수치 재현.
- 신규 이슈 2건(Nit) — 출시 차단 아님.

### R2-1. 이슈별 재검증 (Q-01~Q-29)

| ID | 심각도 | 판정 | 재현·근거 |
|---|---|---|---|
| Q-01 | Major | **닫힘** | `?track=relation` → CALM `시작` → 0.6s 뒤 `처음부터 다시` → **35초 대기**: 봇 말풍선 2("좋아요…", "지금 어떤 행동…"), 사용자 말풍선 0, 블록 1, `.breath` 0, pageerror 0. `HMCoach.session()` 증가 확인. |
| Q-02 | Major | **닫힘** | `?track=money` 로드 직후(<100ms) 재시작 → 봇 말풍선 정확히 2개, 타이핑 인디케이터 0, pageerror 0. 추가 케이스: STOP 응답 타이핑 중(50ms) 재시작 → 2개·step=stop·에러 0. |
| Q-03 | Major | **닫힘** | JS 비활성 컨텍스트: `html.no-js`, index 38/38·relation 28/28 리빌 블록 opacity 1, main 텍스트 2,619자 렌더(`index-1440-nojs.png`). no-JS 에서도 FAQ `<details>` 네이티브 열림 ✓. JS 활성 시 `html.js` 전환·리빌 정상. |
| Q-04 | Major | **닫힘** | 자체 전수 감사(텍스트 노드 기준, 반투명 배경·opacity 합성): 라이트 400/1440 9페이지(44~235 노드) **실패 0**, 다크 1440 9페이지 **실패 0**, 결과 화면 6종 라이트 + 2종 다크 **실패 0**. 토큰 실측: `--ink-3 #67625A`(bg 5.5:1), `--relation #9F4830`(5.6:1), `--amber-ink #7F5C1E`(5.5:1), 다크 `--ink-3 #9A9385`(6.1:1), `.final .lead` 라이트 `--line`(12:1)/다크 `--ink-2`. 12px 미만 텍스트 0(범례 12px). |
| Q-05 | Major | **닫힘** | 관계 트랙 q1 에서 ArrowRight → 카드 유지·값 2·`다음` 활성·포커스 q0 유지; ArrowRight ×3 → 카드 유지(값 4); Tab→`다음`(Enter) → 카드 2·포커스 h2; 라디오에 Space → 카드 유지; **마우스 클릭은 220ms 후 자동 진행** 유지(`checkup-1280-question.png`). |
| Q-06 | Major | **닫힘** | 1.3s 진행 → pause('일시정지') → start: **120ms 시점 '들이쉬세요'**, 320ms '들이쉬세요'. check.js 도 300ms 내 복원 assert 통과. |
| Q-07 | Minor | **닫힘** | 모달에서 Space 시작 → 포커스 `일시정지`; 다시 Space → 포커스 `다시 시작`; 포커스를 body 로 강제 이탈 후 Tab → 모달 첫 요소(`닫기`)로 복귀; body 포커스 상태에서 Esc → 모달 닫힘·포커스가 헤더 호흡 버튼으로 복귀(`index-1440-breath-modal.png`). |
| Q-08 | Minor | **닫힘** | `scroll-padding-top` 76px, `[data-result]` scroll-margin 76px. 결과 보기 후 `[data-result]`/게이지 top **136px** > 헤더 61px(`checkup-1280-result-after-finish.png`). |
| Q-09 | Minor | **닫힘** | `fieldset.likert` border 0 none, padding 0, margin 0(`checkup-1280-question.png`). |
| Q-10 | Minor | **닫힘** | M-35 추천 프로그램 = "머니 마음근력 개인 코칭 [교체: 회기] · 상담 신청 우선" — 지시문 없음. 1336 crisis-note 별도 렌더 유지(`checkup-1280-result-M-35.png`). |
| Q-11 | Minor | **닫힘** | 공유 R-20: "…감정이 행동을 앞지릅니다. **먼저 훈련할 단계부터** 훈련하면…", M-20 동일 패턴. 비공유(응답 있음) R-24: "'알아차림' 단계부터" 그대로. |
| Q-12 | Minor | **닫힘** | 768px: money 헤딩 top < 카드 top(헤딩 먼저), relation 동일. 1440px: aside 가 카드 오른쪽(시각 반전 유지). DOM 순서 aside→카드(`index-768-money.png`). |
| Q-13 | Minor | **닫힘** | `.rail--vertical` 4단계 border-left 전부 0, 2~4단계 border-top 1px, 인라인 style 제거(`about-1440-philosophy.png`). |
| Q-14 | Minor | **닫힘** | sm 원 160px, 카운트 텍스트 129px(12px)·여백 좌우 15px. |
| Q-15 | Minor | **미해결(환경)** | og-image.png 여전히 산세리프 폴백 — 이 세션 폰트 차단. 배포 전 폰트 가능 환경에서 `node tools/make-images.js` 1회 필요(README 체크리스트 기재 확인). 출시 차단 아님. |
| Q-16 | Minor | **닫힘** | SDK 스텁으로 `handler()` 실행: 허용 Origin → 200 + `ACAO: https://huggingmind.kr`; 비허용 Origin POST → **403**·CORS 헤더 없음; 비허용 OPTIONS → 204·CORS 헤더 없음(브라우저 차단); Origin 없는 서버 호출 → 200(레이트리밋 대상). IP 키 `x-nf-client-connection-ip` 우선. 동일 IP 12회째 → **429**. 인메모리 한계 주석·README 명시 확인. |
| Q-17 | Minor | **닫힘** | 500자 action → 120자, emotions 5개(숫자 포함) → 문자열 3개, intensity 99/'7' → null, need 객체 → null; 배열/`null` payload → 400; 9KB body → 413; 키워드 → `safety_flag:true`. 요청 파라미터: `claude-opus-5`, `thinking:{type:'adaptive'}`, `output_config.effort:'low'+format json_schema`, `fallbacks:'default'`, `betas:['server-side-fallback-2026-07-01']`(claude-api 스킬과 일치). `COACH_MODEL` env 반영(Q-28). |
| Q-18 | Minor | **닫힘** | 칩 Enter → 포커스 `다음: 감정 찾기`(CTA); CTA Enter → 포커스 첫 감정 칩 `불안`. 위기 카드 시 tel 링크로 이동 로직 확인. |
| Q-19 | Minor | **닫힘** | 폰트 CSS `preload as=style → onload stylesheet` + `<noscript>`, preconnect 2개, 웨이트 7→6(Mono 500 제거). 차단 환경에서 콘솔 에러 0(36회 로드). 실배포 후 폰트 로드 확인 권장. |
| Q-20 | Nit | **닫힘** | `.flow > .icon` 색 `rgb(103,98,90)`(=ink-3) 적용, `.tag--accent`·`.mb` 제거, `.chat__field`·`.nvc label` CSS 화, 9페이지 body 인라인 `style` **0건**. |
| Q-21 | Nit | **닫힘** | 400px: `.flow__mark::after` position static, 마크가 전폭(310px) 행으로 분리되어 세로 화살표 + 라벨 정적 배치(`index-400-s2-flow.png`). |
| Q-22 | Nit | **닫힘** | 768px 히어로 2열(334/334px), 미디어 우측 정렬 폭 334px(`index-768-hero.png`). |
| Q-23 | Nit | **닫힘** | 공유 결과 로드 시 h2 포커스 상태에서 outline `none`. |
| Q-24 | Nit | **예외 인정** | 입력 outline-offset 1px — 인접 필드 겹침 사유 타당. 02-design 예외 기록 필요(B봇). |
| Q-25 | Nit | **예외 인정** | S4/S5 카드 Ghost 링크 생략 — 링크 중복 회피 사유 타당. |
| Q-26 | Nit | **닫힘** | coach-data NVC 4칸 "~했을 때(관찰)" 등 기획 원문. |
| Q-27 | Nit | **예외 인정** | S6 카드 인라인 타이머 `시작` 유지 — 기획 조항 부합. |
| Q-28 | Nit | **닫힘** | `MODEL = process.env.COACH_MODEL \|\| 'claude-opus-5'`, .env.example 주석 갱신. |
| Q-29 | Nit | **닫힘** | check.js: privacy 포함 36회, 요소 rect 오버플로, 대비(라이트 36회+다크 9회), no-JS, 호흡 재개, 모달 포커스/Esc, 자가진단 키보드, 공유 문구, 안정 CTA, 재시작 레이스 2종, 칩 포커스, FAQ/FAQPage, 푸터 h2 — 재실행 73 ok/0 fail. |

### R2-2. 신규 추가분 검수

| 항목 | 판정 | 근거 |
|---|---|---|
| FAQ 아코디언(relation/money 각 5) | **PASS** | 네이티브 `<details>/<summary>`: Tab 으로 summary 포커스(outline 2px), Enter 열림 → Space 닫힘(토글), 첫 항목만 `open`, 나머지 4개 닫힘. 접근성 트리: 각 항목 group + 열린 항목 본문 paragraph 노출. no-JS 에서도 열림. 400px 줄바꿈·아이콘 정렬 정상(`relation-400-faq.png`, `relation-1280-faq.png`). |
| FAQPage JSON-LD | **PASS** | 두 페이지 `@graph` 에 `FAQPage`(`@id …#faq`, `isPartOf` WebPage) 1개, `mainEntity` Question 5 × `acceptedAnswer.text`. **표시 텍스트와 스키마 name/text 10/10 완전 일치**(HTML 엔티티 복원 후 비교). 파싱 오류 없음. |
| 자가진단 "오늘 할 것 1개" | **PASS** | 6조합 렌더: R 안정 "먼저 아이(배우자)의 말을…되풀이해 주기" / 주의 "20분 뒤 다시 이야기하자고 알리기" / 훈련 "상처를 한 줄로 적어 두고…", M 안정 "30초 점검하기" / 주의 "손절선·투자 한도를 지금 적어 두기" / 훈련 "거래 앱을 닫고 24시간 뒤에 열기" — 전부 coach-data CHOOSE 선택지 원문. 안정 구간 primary CTA "AI 코치로 주 1회 점검하기", 그 외 primary "상담 신청". 다크·라이트 대비 통과. |
| 푸터 헤딩 변경 | **PASS** | 9페이지 `footer h2` 0, `p.site-footer__heading` 3개(프로그램/도구/소개·문의). main h2 구조 유지(index 7개). |
| 교차 링크 | **PASS** | relation → `money.html`("머니 마음근력 — 손실 후 감정조절·추격매매 멈추기"), money → `relation.html`, about 확장성 → 두 트랙, ai-coach 사이드 → 두 트랙. 링크 대상 존재. 섹션 번호 relation 01~06(FAQ 05, 미니 도구 06), money 01~06(FAQ 06) 연속. |
| h1 보강 | **PASS** | relation h1 접근성 이름 "다시, 우리. 부부·부모자녀 화 조절 훈련"(블록 span 이라 SR 이 공백 삽입), 서브 37px(.5em). ai-coach/checkup h1 접두 추가. h1 각 1개 유지. |
| nav.js 히스테리시스 | **PASS** | 1440/400 모두: y=47 미축소 → 49 축소 → 30 유지(축소) → 13 유지 → 11 복원. 임계 근처 왕복(20→40→50→44→52→46→60→55→70) 클래스 변경 **0회**, y=50 고정 시 1회, `#coach` smooth scroll 중 변경 **1회**(51px 에서 축소 후 진동 없음). |

### R2-3. 회귀·카피

- `tools/check.js`: 36/36(9페이지×4폭) PASS — 콘솔 에러 0, 4xx 0, scrollWidth/요소 오버플로 0, h1 1, 위기 연락처, `html.js`, 대비 실패 0(예: index 400 225/225) · 다크 9/9 · no-JS 38/38 · 호흡 30.02s·재개 문구·모달 포커스 · 자가진단 경계값/키보드/포인터/오늘 할 것/공유 문구/안정 CTA · 코치 3트랙·CALM 30.59s·위기 카드 2종·레이스 2종·칩 포커스 · 테마·오버레이·폼·FAQ·푸터 = **73 ok / 0 fail**.
- 자체 재실행: 36회 페이지 로드 콘솔 에러·pageerror·4xx 0, 요소 오버플로 0. 테마 토글 저장 ✓, reduced-motion 리빌·히어로 즉시 표시 ✓, 폼 빈 제출 오류 5 ✓, 미설정 안내 `<pre>` CSS 이관 후 스타일 유지(pre-wrap, 1px border) ✓, 코치 흐름 네트워크 요청 동일 출처 정적 파일 외 0건 ✓.
- 1라운드 PASS 항목(§2 #1·2·4·5·9·10) 재확인 — 변동 없음.
- **카피 diff(HEAD~1→HEAD, body 텍스트 추출 비교)**: index — 히어로 칩 한글 병기(E봇 P1)와 S5 DOM 재배치(Q-12, 텍스트 동일)뿐; relation — h1 서브라인·FAQ 5문답·섹션 번호(05 미니 도구→05 FAQ/06 미니 도구)·교차 링크; money — FAQ 5문답·교차 링크; ai-coach/checkup — h1 접두·트랙 안내 링크; about — 확장성 링크; contact/404/privacy — **0건**. 승인 범위 밖 카피 변경 **없음**. FAQ 문안은 06-seo §7-1 원문(E봇)이며 브리프 외 사실(연락처·수치·후기) 없음, 1336 만 공공 연락처로 포함.

### R2-4. 신규 이슈 (Q-30~)

**Q-30 · Nit · `site/assets/css/base.css:49`, `components.css:50`** — Q-19 로 IBM Plex Mono 500 웨이트를 제거하면서 `.mono/.eyebrow/.wordmark__brand` 가 400 으로 바뀜. 02-design §2·§4(모노 500, 워드마크 500 13px)와 상이 — 성능 사유는 타당하므로 B봇이 디자인 문서에 예외로 반영하거나, 워드마크만 `font-weight:500`(합성 볼드 없이 시스템 폴백)으로 둘지 결정 필요.

**Q-31 · Nit · `tools/check.js` contrastAudit** — 배경 판정이 "조상 중 첫 불투명 background-color" 이고 실패 8건에서 중단하므로 반투명 배경(`--header-bg` 90%)·opacity 합성 케이스는 근사치. 이번 라운드는 D봇 전수 감사(반투명 합성 포함)와 결과가 일치(모두 0 실패)했으나, 토큰을 다시 바꿀 때는 두 감사를 함께 돌릴 것.

### R2-5. 01-plan §8 성공 기준 최종 판정

| # | 항목 | R1 | **R2 최종** | 근거 |
|---|---|---|---|---|
| 1 | 4폭 가로 스크롤 없음·헤더 CTA·햄버거 | PASS | **PASS** | 36/36, 요소 오버플로 0, 오버레이 트랩/Esc ✓ |
| 2 | 히어로 h1 슬로건, 페이지당 h1 1개 | PASS | **PASS** | index h1 원문 유지, 9페이지 h1=1 |
| 3 | 다크/라이트 자동+토글 저장, 본문 대비 4.5:1, Lighthouse ≥95 | 부분 | **PASS(대비) / Lighthouse 미측정** | 시스템 다크 반영·저장 ✓, 라이트·다크 텍스트 노드 전수 4.5:1 실패 0. Lighthouse 는 미설치로 실측 불가 — 대비·label·alt·heading·landmark·contrast 항목을 수동 확인했으며 감점 요인을 찾지 못함. 배포 후 실측 1회 권장. |
| 4 | 코치 v1 네트워크 차단 3트랙 완주·요약 카드·키워드 위기 카드 | PASS | **PASS** | 3트랙 완주, 네트워크 0건, 위기 카드 즉시, 재시작 레이스 해소 |
| 5 | 자가진단 8×2·미응답 차단·경계값·6문구 | PASS | **PASS** | 경계값 6종·6문구·지시문 미노출·오늘 할 것 |
| 6 | 호흡 30±0.5·일시정지/중단/재시작·reduced-motion·CALM 콜백 | 부분 | **PASS** | 30.02s, 재개 120ms 내 문구 복원, 스케일 미적용, 콜백 30.59s |
| 7 | 폼 2종 오류·전화 패턴·honeypot·확인 메시지(mailto 폴백) | 부분 | **PASS(조건부)** | 검증·honeypot·안내 모드 ✓. mailto 폴백 코드 경로 존재하나 실이메일(`data-mailto`) 미설정 — 사용자 값 입력 후 1회 확인 필요 |
| 8 | 키보드 전 조작·포커스 링·트랩·alt·label·스킵 링크 | 부분 | **PASS** | 모달 포커스 관리·Esc, 자가진단 키보드 자동 진행 금지, 칩 포커스 이동, FAQ 키보드 조작 확인 |
| 9 | 메타/OG/canonical/favicon/sitemap/robots·콘솔 에러·404 0 | PASS | **PASS** | 변동 없음, 에러 0. og-image 세리프 재생성만 남음(Q-15) |
| 10 | 브리프 외 사실 없음·[교체] 목록·위기 연락처 노출 | PASS | **PASS** | 카피 diff 로 무단 변경 없음 확인, FAQ 도 사실 추가 없음, 위기 4종 유지 |

### R2-6. 스크린샷 (`scratchpad/qa-shots-r2/`, 62장)

페이지×폭(라이트, fullPage): `{index,relation,money,ai-coach,checkup,about,contact,404,privacy}-{400,768,1024,1440}.png`
다크: `index-1440-dark.png` `relation-1440-dark.png` `money-1440-dark.png` `checkup-1440-dark.png` `ai-coach-1440-dark.png`
이슈 검증: `index-1440-nojs.png`(Q-03) `checkup-1280-question.png`(Q-05·Q-09) `index-1440-breath-modal.png`(Q-07) `checkup-1280-result-after-finish.png`(Q-08) `checkup-1280-result-M-35.png`(Q-10·오늘 할 것) `checkup-1280-result-R-20.png`(Q-11) `index-768-money.png`(Q-12) `about-1440-philosophy.png` `about-1440-rail.png`(Q-13) `ai-coach-1280-calm.png`(Q-14) `ai-coach-1280-feel.png`(Q-18) `index-400-s2-flow.png`(Q-21) `index-768-hero.png`(Q-22) `contact-1280-errors.png` `index-1280-breath-modal.png`
신규 기능: `relation-1280-faq.png` `relation-1440-faq.png` `relation-400-faq.png` `money-1440-crosslink.png`
데이터: `qa-r2-results.json`(Major 재현·대비 전수·오버플로·신규 기능·회귀) `qa-r2c-results.json`(Q-08/09·네트워크·nav 타임라인) · C봇 check.js 재실행 `scratchpad/cshots-r2/`, `scratchpad/check-run-r2.log` · 서버리스 함수 스텁 테스트 `scratchpad/fn-test/run.mjs`

### R2-7. 5줄 요약

1. 최종 판정 **출시 가능** — Q-01~Q-29 중 닫힘 25, 의도적 예외 3(타당), 환경 제약 1(og-image 세리프, 배포 전 재생성). 회귀 0.
2. Major 6건 Playwright 직접 재현으로 닫힘: 호흡 중 재시작 35초 후 유령 말풍선 0·pageerror 0, no-JS 38/38 렌더, 라이트·다크 텍스트 노드 전수 4.5:1 실패 0, 자가진단 키보드 자동 진행 없음, 호흡 재개 120ms 내 문구 복원, 모달 포커스/Esc 정상.
3. `tools/check.js` 보강판 재실행 36/36 PASS · 73 ok · 0 fail(호흡 30.02s, CALM 30.59s); 서버리스 함수는 SDK 스텁으로 CORS 403·sanitize·413·429·keyword 경로 확인.
4. 신규 FAQ(키보드·AX 트리·no-JS·FAQPage 스키마 10/10 텍스트 일치)·오늘 할 것 6조합·푸터 헤딩·교차 링크·h1 보강·nav 히스테리시스(임계 왕복 시 클래스 변경 0회) 모두 PASS; 카피 diff 상 승인 범위 밖 변경 없음.
5. 신규 이슈 Nit 2건(Q-30 모노 500 제거 디자인 예외 기록, Q-31 check.js 대비 감사 근사) — 출시 차단 아님. 성공 기준 10개: PASS 10(3번 Lighthouse 는 미측정·대비 전수 통과, 7번 mailto 실값 입력 후 확인).
